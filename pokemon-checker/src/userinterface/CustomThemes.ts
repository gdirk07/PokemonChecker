import { createTheme } from "@mui/material/styles";

export const shinyTheme = createTheme({
  palette: {
    primary: {
      main: '#ffffff',
    },
    secondary: {
      main: '#ffff00',
    },
  }
})